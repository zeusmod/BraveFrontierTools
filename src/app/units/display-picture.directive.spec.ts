import { DisplayPictureDirective } from './display-picture.directive';

describe('DisplayPictureDirective', () => {
  it('should create an instance', () => {
   /* const directive = new DisplayPictureDirective();
    expect(directive).toBeTruthy();*/
  });
});
